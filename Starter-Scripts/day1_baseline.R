# Day 1 starter: baseline task
# Repeat an equivalent version of this script on Day 7 (see day7_baseline.R).

baseline <- read.csv("Data/raw/day1_baseline_sample.csv")

# TODO: inspect the structure of baseline (str(), summary())

# TODO: identify missing or invalid values in test_score and attendance_pct
# Hint: is.na(), and logical conditions like baseline$test_score < 0

# TODO: produce one numerical summary (e.g. mean test_score, na.rm = TRUE)

# TODO: produce one labelled plot, e.g.
# hist(baseline$test_score, main = "Day 1 baseline: test scores", xlab = "Score")

# TODO: write three sentences interpreting the result for a non-technical
# reader, and save them to Outputs/day1_baseline_summary.txt
