# Data

Use `raw/` for unmodified source data and `processed/` for reproducible outputs from cleaning scripts. The workshop should use one anchor dataset from Day 2 through Day 7; label any other dataset as a demonstration dataset.
